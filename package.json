{
  "name": "getrak-core-mcp",
  "version": "0.1.0",
  "description": "Getrak Core MCP — servidor MCP que expõe um catálogo curado de tools de leitura sobre a Getrak API Core.",
  "type": "module",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.1109.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "ioredis": "^6.0.0",
    "zod": "^3.24.1",
    "zod-to-json-schema": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
